# Copy to deploy/server.env on the server to change port without editing systemd.
# Default production port is 9030 (8080 is often already in use).
WEBHOOK_SECRET=change-me-to-a-long-random-string
WEBHOOK_PORT=9001
WEBHOOK_BRANCH=main
APP_DIR=/opt/device-safety-manager
